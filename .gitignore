node_modules/
.next/
out/
dist/
coverage/
*.tsbuildinfo

# Next.js generates this; it churns between dev/build runs
next-env.d.ts

.env
.env.local
.env.*.local

.DS_Store
*.log
npm-debug.log*
.vercel

# Personal, machine-local Claude Code config (shareable config lives in .claude/*.json)
.claude/settings.local.json

# Built agent-kit archive (regenerated by npm run build:kit)
kit-dist/
